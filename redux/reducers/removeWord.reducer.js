
const removeWord = (state = {}, s) => {
    // const {payload, type} = s;
    // if(type === 'DELETE_WORD')
    //      {
    //         console.log(state);
    //         // const {payload, ...rest} = state;
    //         return state;
    //     }
    //   else
          return state;
};

export default removeWord;